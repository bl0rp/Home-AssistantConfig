alias: 'mqtt pushover test'
trigger:
  platform: mqtt
  topic: 'notify'
  payload: 'pushover_test'

action:

 - service: notify.pushover
   data_template:
     title: "Via MQTT"
     message: "Test Message. temp. elw: {{ states('sensor.temperatur_esp8266_02')|round(1)|replace('.', ',') }} °C"
     data:
       sound: falling
       device: blabla
       url: "https://home-assistant.io/"
       priority: 0
       # priority: 2
       # retry: 60
       # expire: 3600