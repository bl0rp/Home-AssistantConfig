alias: New Public IP
trigger:
  - platform: state
    entity_id: sensor.myipopendnscom
action:
  - service: notify.pushbullet
    data_template:
      title: "New IP!"
      message: >
         Die IPv6 Adresse hat sich von {{ trigger.from_state.state }} zu {{ trigger.to_state.state }} geändert!