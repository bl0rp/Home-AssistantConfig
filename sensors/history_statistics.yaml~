###########################
#### History Stats ########
###########################

###Lampen/Schalter
  - platform: history_stats
    name: Bettlicht AN Heute
    entity_id: switch.110111
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Bettlicht AN Gestern
    entity_id: switch.110111
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: Bettlicht AN Woche
    entity_id: switch.110111
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Bettlicht AN letzte 30 Tage
    entity_id: switch.110111
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: Bettlicht AN seit Anbeginn der Zeit
    entity_id: switch.110111
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Schreibtischlampe AN Heute
    entity_id: switch.110112
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Schreibtischlampe AN Gestern
    entity_id: switch.110112
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: Schreibtischlampe AN Woche
    entity_id: switch.110112
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Schreibtischlampe AN letzte 30 Tage
    entity_id: switch.110112
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: Schreibtischlampe AN seit Anbeginn der Zeit
    entity_id: switch.110112
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Türlicht AN Heute
    entity_id: switch.110113
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Türlicht AN Gestern
    entity_id: switch.110113
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: Türlicht AN Woche
    entity_id: switch.110113
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Türlicht AN letzte 30 Tage
    entity_id: switch.110113
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: Türlicht AN seit Anbeginn der Zeit
    entity_id: switch.110113
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: MPD 433MHz AN Heute
    entity_id: switch.110011
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: MPD 433MHz AN Gestern
    entity_id: switch.110011
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: MPD 433MHz AN Woche
    entity_id: switch.110011
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: MPD 433MHz AN letzte 30 Tage
    state: 'on'
    entity_id: switch.110011
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: MPD 433MHz AN seit Anbeginn der Zeit
    entity_id: switch.110011
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: RPi IR Cam AN Heute
    entity_id: switch.110012
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: RPi IR Cam AN Gestern
    entity_id: switch.110012
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: RPi IR Cam AN Woche
    entity_id: switch.110012
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: RPi IR Cam AN letzte 30 Tage
    entity_id: switch.110012
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: RPi IR Cam AN seit Anbeginn der Zeit
    entity_id: switch.110012
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: IR Strahler AN Heute
    entity_id: switch.110013
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: IR Strahler AN Gestern
    entity_id: switch.110013
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: IR Strahler AN Woche
    entity_id: switch.110013
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: IR Strahler AN letzte 30 Tage
    entity_id: switch.110013
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: IR Strahler AN seit Anbeginn der Zeit
    entity_id: switch.110013
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Lampe Esszimmer AN Heute
    entity_id: switch.wemoswitch1
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Lampe Esszimmer AN Gestern
    entity_id: switch.wemoswitch1
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: Lampe Esszimmer AN Woche
    entity_id: switch.wemoswitch1
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Lampe Esszimmer AN letzte 30 Tage
    entity_id: switch.wemoswitch1
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: Lampe Esszimmer AN seit Anbeginn der Zeit
    entity_id: switch.wemoswitch1
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Stehlampe AN Heute
    entity_id: switch.wemoswitch2
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Stehlampe AN Gestern
    entity_id: switch.wemoswitch2
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: Stehlampe AN Woche
    entity_id: switch.wemoswitch2
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Stehlampe AN letzte 30 Tage
    entity_id: switch.wemoswitch2
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: Stehlampe AN seit Anbeginn der Zeit
    entity_id: switch.wemoswitch2
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'

###Device Tracker
  - platform: history_stats
    name: Christoph Anwesenheit Heute
    entity_id: input_boolean.chrishome
    state: 'on'
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Christoph Anwesenheit Gestern
    entity_id: input_boolean.chrishome
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      hours: 24

  - platform: history_stats
    name: Christoph Anwesenheit Woche
    entity_id: input_boolean.chrishome
    state: 'on'
    start: '{{ as_timestamp( now().replace(hour=0).replace(minute=0).replace(second=0) ) - now().weekday() * 86400 }}'
    end: '{{ now() }}'

  - platform: history_stats
    name: Christoph Anwesenheit letzte 30 Tage
    entity_id: input_boolean.chrishome
    state: 'on'
    end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    duration:
      days: 30

  - platform: history_stats
    name: Christoph Anwesenheit seit Anbeginn der Zeit
    entity_id: input_boolean.chrishome
    state: 'on'
    start: '{{ 0 }}'
    end: '{{ now() }}'